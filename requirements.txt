flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0
# Pinned: phase2_xgboost_calibrated.joblib is a pickle of a CalibratedClassifierCV.
# Unpickling under a different scikit-learn minor version warns and can fail, so
# these four match the versions the model was trained and pickled with.
numpy==2.2.6
scipy==1.16.0
joblib==1.5.1
xgboost==3.2.0
scikit-learn==1.8.0
